{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts --outdir dist"
  },
  "dependencies": {
    "@ifyour/deeplx": "^0.1.14",
    "hono": "^3.10.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230914.0",
    "wrangler": "^3.14.0"
  }
}